---
import { waLink } from "@/config";
import { cn } from "@/lib/utils";

interface Props {
  /** Mensaje ya prellenado (usa waServiceMessage / waDefaultMessage) */
  message: string;
  label?: string;
  size?: "lg" | "md";
  /** Variante visual: sólida o contorno */
  variant?: "solid" | "outline";
  class?: string;
}

const {
  message,
  label = "WhatsApp",
  size = "md",
  variant = "solid",
  class: className,
} = Astro.props;
---

<a
  href={waLink(message)}
  target="_blank"
  rel="noopener"
  class={cn(
    "inline-flex items-center justify-center rounded-sm font-semibold uppercase leading-none tracking-[0.06em] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring active:translate-y-px",
    variant === "solid" ? "bg-foreground text-background" : "border border-border text-foreground",
    "h-12 px-7 text-sm",
    className,
  )}
>
  <span class="hover-flip inline-block">
    <span class="flip-a block">{label}</span>
    <span aria-hidden="true" class="flip-b block">{label}</span>
  </span>
</a>
